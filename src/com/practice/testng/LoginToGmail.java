package com.practice.testng;
/*package com.edureka.testng;

import org.openqa.selenium.By;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.testng.Assert;
import org.testng.annotations.Test;

public class LoginToGmail extends BaseTest {
	
		@Test(invocationCount=100)
		public void login()
		{
			WebElement email = driver.findElement(By.id("identifierid"));
			email.sendKeys("seleniumwebpractice@gmail.com");
			driver.findElement(By.xpath("//span[text()='Next']")).click();
			Assert.fail();
			driver.findElement(By.name("password")).sendKeys("Practice456");
			driver.findElement(By.xpath("//span[text()='Next']")).click();
			String exp_title = "";
					
					wait.until(ExpectedConditions.titleContains("Inbox"));
					String title = driver.getTitle();
					
					String payeeStatus = driver.findElement(By.xpath("")).getText();
					Assert.assertNotNull(payeeStatus);
					
					Assert.assertEquals(actual,expected);
					Assert.assertSame(actual,expected);
					if(title.contains("Inbox"))
						
					{
						System.out.println("Login was successful");
					}
					else
					{
						System.out.println("Login failed");
					}
		}
  

	}
*/
